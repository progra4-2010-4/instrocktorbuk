<%unless current_user!=false%>

            
           
	   <%else%>
	  
            <figure contenteditable="true">
            <img src=<%=current_user.picture_url%> alt="jump, matey" contenteditable="false" />
            <figcaption contenteditable="false">Jump!</figcaption>
            </figure>
	   <%end%>
