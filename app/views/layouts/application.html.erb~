<!DOCTYPE html>
<html>
<head>
  <title>Instrocktorbuk</title> 
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
	<div id="fondo">
		<header>
			<div class="main">
			<img src="jojo.gif" width="28" height="21" alt="Tamaño original" border="3">
				<h1 align="center"><%=link_to "INSTROCKTORBUK", '/'%></h1>
				<%unless current_user!=false%>
					<%=link_to "Login", '/login'%>
		   		<%else%>
					<%=current_user.name%>
					<%= link_to "Logout",'/logout' %>
					<%= link_to "Upload photo", '/user/new'%>
		   		<%end%>
	   		</div>
		</header>
		<%= yield %>
	</div>
</body>
</html>
